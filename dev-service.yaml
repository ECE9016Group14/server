apiVersion: v1
kind: Service
metadata:
  name: dev-web-service
spec:
  type: LoadBalancer
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80         # External port
      targetPort: 8000 # Web container listens on 8000
